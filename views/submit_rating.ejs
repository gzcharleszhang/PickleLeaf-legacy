<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/bootstrap.css' />
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='shortcut icon' href='/images/favicon.ico' type='image/x-icon'/>
</head>

<body>
<% include templates/navigation.ejs%>
<% include templates/flash.ejs %>
<% if (errors.length > 0){ %>
<% errors.forEach(function(item){ %>
<div class="alert alert-danger"><%=item.msg%></div>
<%})%>
<%}%>
<h3 class="page-header">Submit a Rating</h3>
<form method="post" action="<%='/submit/rating/' + receiverId%>">
    <div class="form-group text-center">
        <label>Comment</label>
        <input type="text" class="form-control" name="comment" placeholder="Comment">
    </div>
    <div class="form-group text-center">
        <label>Rating (1 - 5)</label>
        <input type="number" class="form-control" name="score" placeholder="Rating" min="1" step="1" max="5">
    </div>
    <div class="form-group text-center button">
        <button type="submit" class="btn btn-primary">Submit</button>
    </div>
</form>
<% include templates/footer.ejs%>
</body>
</html>